{
  "name": "sms-api-lambda",
  "version": "1.0.0",
  "type": "module",
  "description": "Lambda thin: composition root para resolvers AppSync de configuración multi-tenant",
  "main": "src/index.js",
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@sms/application": "*",
    "@sms/domain": "*",
    "@sms/infrastructure": "*"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.4.5"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
