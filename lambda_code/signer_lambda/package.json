{
  "name": "sms-signer-lambda",
  "version": "1.0.0",
  "type": "module",
  "description": "Lambda thin: composition root para presigned URLs de upload",
  "main": "src/index.js",
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@sms/application": "*",
    "@sms/infrastructure": "*"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.4.5"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
